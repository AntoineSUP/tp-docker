- name: Run HTTPD container
  community.docker.docker_container:
    name: proxy
    image: 1oooka/tp-devops-httpd:latest
    state: started
    restart_policy: always
    networks:
      - name: app_network
    ports:
      - "80:80"