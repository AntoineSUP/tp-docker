all:
  vars:
    ansible_user: admin
    ansible_ssh_private_key_file: ~/.ssh/id_rsa_takima
    ansible_python_interpreter: /usr/bin/python3
  children:
    prod:
      hosts:
        antoine.jarmoszko.takima.school:
